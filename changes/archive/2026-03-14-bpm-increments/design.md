# Design: BPM Increment Steps
**Status: Approved** *(retrospective)*

## Approach

Two constant changes in the `BpmIncrease`/`BpmDecrease` and `BaseBpmIncrease`/`BaseBpmDecrease` action handlers in `tui_loop`. Help text updated to match.

## Tasks

1. ✓ **Impl**: `BpmIncrease`/`BpmDecrease` step: `0.1 → 1.0`; `BaseBpmIncrease`/`BaseBpmDecrease` step: `0.01 → 0.1`; help text updated
2. ✓ **Process**: Archive
