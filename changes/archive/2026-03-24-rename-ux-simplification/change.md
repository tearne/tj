# Rename UX Simplification
**Type**: Fix
**Status**: Approved

## Log

Remove the `y` quick-accept path and make the tag editor modal the single rename entry point. `y` now opens the editor (previously `h`); `h` is removed. The rename offer display text changes from `rename? [y/h]` to `rename? [y]`.
